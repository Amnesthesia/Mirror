import { Record } from 'immutable';

const TitleRecord = Record({
  label: '',
  style: null,    // SCROLLING, FADE_IN, FADE_OUT, FADE_IN_OUT, SLIDE_IN, SLIDE_OUT, SLIDE_IN_OUT, null
});

export { TitleRecord };
